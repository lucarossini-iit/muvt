#include <ros/ros.h>
#include <ros/publisher.h>
#include <ros/node_handle.h>

#include <visualization_msgs/Marker.h>

#include <Eigen/Geometry>

class Obstacle
{
public:
    Obstacle(Eigen::Affine3d pose);

private:
    double _x, _y, _z;
    Eigen::Matrix3d _rot;
    std::string _name;

    ros::NodeHandle _nh;
    ros::Publisher _pub;
};

