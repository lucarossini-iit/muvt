# muvt

## dependencies:

```
sudo apt-get install libsuitesparse-dev \
                     ros-${ROS_DISTRO}-ompl \
                     ros-${ROS_DISTRO}-libg2o \
                     ros-${ROS_DISTRO}-octomap-ros \
                     -y
```
